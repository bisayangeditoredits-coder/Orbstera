// Smooth scroll (Lenis) has been disabled in favour of native instant scroll.
// The component is kept as a no-op so existing import sites don't break.
export function SmoothScroll() {
  return null;
}
